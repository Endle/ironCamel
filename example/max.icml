
fn larger(a, b) {
    if >(a,b) then {a} else {b}
}

fn main() {
    let y = list(100, 4, 5, 6, 92);

    let a = fold(y, +);

    let b = fold(y, larger);
    writeline@stdout<<a;
    writeline@stdout<<b;
    0
}
