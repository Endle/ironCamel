fn main() {
    fopen_read @ fin = "example/magic_number.txt";
    readstr @ fin >> s;
    let slist = strtok(s, " \t");

    writelist @ stdout << slist;
    //let y = atoi(s);
    //let z = *(y, 10);
    //writeline @ stdout << s;
    //writeline @ stdout << z;

    0
}