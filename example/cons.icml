

fn main() {
    //readline @ stdin >> a;
    let x = 5;
    let y = list(4, x, 6);
    let z = cons(x, list(get_magic_number()));
    writelist @ stdout << y;
    writelist @ stdout << z;
    //writeline@stdout<<get_magic_number();
    0
}
